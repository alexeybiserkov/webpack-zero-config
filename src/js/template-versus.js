const comments = require('./components/comments');
const mainMenu = require('./components/main-menu');
const sidebarExtended = require('./components/sidebar-extended');
const smoothScrolling = require('./components/smooth-scrolling');
const stickyCtaBar = require('./components/sticky-top-bar');

comments.init();
mainMenu.init();
sidebarExtended.init();
smoothScrolling.init();
stickyCtaBar.init();
